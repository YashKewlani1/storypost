# env files — never commit these
.env
.env.local
.env.*.local

# dependencies
node_modules/

# build output — dist is committed so Vercel can serve it without a build step
# frontend/dist/
frontend/.vite/

# OS
.DS_Store

# Local sensitive files
server/.env
frontend/.env
.claude/settings.local.json
.env
.vercel
